# ── Supabase (auth + database) ─────────────────────────────────────────────
# Create a free project at https://supabase.com
# Copy from: Project Settings → API
REACT_APP_SUPABASE_URL=https://your-project-id.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-public-key
# Service-role key (Project Settings → API → service_role). SERVER-ONLY — never
# expose this in the client bundle. Required for account self-deletion
# (/api/delete-account), which removes the auth user and cascades their data.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ── AI backend ──────────────────────────────────────────────────────────────
# Gemini API key from https://aistudio.google.com
GEMINI_API_KEY=your-gemini-api-key

# ── Analytics (optional) ────────────────────────────────────────────────────
# PostHog project API key from https://posthog.com (free tier available)
REACT_APP_POSTHOG_KEY=phc_xxxxxxxxxxxx
# REACT_APP_POSTHOG_HOST=https://us.i.posthog.com   # default, change for EU

# ── Error monitoring (optional) ─────────────────────────────────────────────
# Sentry DSN from https://sentry.io (free tier available)
REACT_APP_SENTRY_DSN=https://xxxx@xxxx.ingest.sentry.io/xxxx

# ── Email (optional) ────────────────────────────────────────────────────────
# Resend API key from https://resend.com (3k emails/month free)
RESEND_API_KEY=re_xxxxxxxxxxxx
RESEND_FROM=Koala Statements <hello@koalastatements.com>

# ── Contact / Feedback form via EmailJS ─────────────────────────────────────
# Create a free account at https://www.emailjs.com — add an Email Service and an
# Email Template, then copy the IDs + Public Key from the dashboard.
# NOTE: REACT_APP_* vars are embedded in the client bundle. The EmailJS Public
# Key is designed to be public, but lock the template down in the EmailJS
# dashboard (allowlist your domain) to prevent abuse.
REACT_APP_EMAILJS_SERVICE_ID=service_xxxxxxx
REACT_APP_EMAILJS_TEMPLATE_ID=template_xxxxxxx
REACT_APP_EMAILJS_PUBLIC_KEY=xxxxxxxxxxxxxxxx

# ── Rate limiting (optional, recommended for production) ─────────────────────
# Upstash Redis from https://upstash.com (10k commands/day free)
UPSTASH_REDIS_REST_URL=https://xxxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=xxxxxxxxxxxx

# ── Koala Pro billing via Lemon Squeezy (unlocks the AI features) ───────────
# Lemon Squeezy is a Merchant of Record: it charges the customer, collects &
# remits global VAT/sales tax, and pays you out — and it supports Israel-based
# accounts (Stripe does not). The AI paywall stays OFF until BOTH the client flag
# below AND LEMONSQUEEZY_API_KEY are set, so the app runs exactly as before until
# you complete this section.
#
# Setup (https://app.lemonsqueezy.com):
#   1. Create your store, then a Product "Koala Pro" with two subscription
#      variants: $9 / month and $90 / year. Open each variant and copy its
#      numeric id → LEMONSQUEEZY_VARIANT_MONTHLY / LEMONSQUEEZY_VARIANT_YEARLY.
#   2. Settings → Stores: copy the Store ID → LEMONSQUEEZY_STORE_ID.
#   3. Settings → API: create an API key → LEMONSQUEEZY_API_KEY.
#   4. Settings → Webhooks → add a webhook:
#        URL:    https://YOUR_DOMAIN/api/lemonsqueezy-webhook
#        Signing secret: your choice → LEMONSQUEEZY_WEBHOOK_SECRET
#        Events: subscription_created, subscription_updated,
#                subscription_cancelled, subscription_expired,
#                subscription_paused, subscription_unpaused, subscription_resumed
#   5. Run supabase-setup.sql (it includes the `subscriptions` table).
#   6. Requires SUPABASE_SERVICE_ROLE_KEY (above) so the webhook can write plans.
#
# Client flag — turns the paywall UI on. "1" to enable, unset/anything else = off.
REACT_APP_BILLING_ENABLED=0
# Server-only Lemon Squeezy config (never REACT_APP_*, never in the client bundle):
LEMONSQUEEZY_API_KEY=eyxxxxxxxx
LEMONSQUEEZY_STORE_ID=12345
LEMONSQUEEZY_VARIANT_MONTHLY=123456
LEMONSQUEEZY_VARIANT_YEARLY=123457
LEMONSQUEEZY_WEBHOOK_SECRET=your-webhook-signing-secret
